{
  "name": "nimrox-restserver",
  "projectType": "application",
  "root": "apps/my-java-app",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn package"
      },
      "outputs": ["apps/nimrox-restserver/target"]
    },
    "run": {
      "executor": "nx:run-commands",
      "options": {
        "command": "mvn exec:java"
      }
    }
  },
  "tags": []
}
